{
  "name": "Variphi Annotation Studio",
  "description": "Multi-type data labeling, annotation and exploration tool",
  "keywords": [
    "data annotation",
    "data labeling"
  ],
  "website": "variphi.com",
  "repository": "https://github.com/VariPhiGen/variphi-label-studio.git",
  "logo": "",
  "stack": "container",
  "env": {
    "DISABLE_SIGNUP_WITHOUT_LINK": {
      "description": "Disable signup for users without invite link",
      "value": "0",
      "required": false
    },
    "USERNAME": {
      "description": "Username(email) for default user",
      "value": "",
      "required": false
    },
    "PASSWORD": {
      "description": "Password for default user",
      "value": "",
      "required": false
    }
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "docker build -f Dockerfile.cloudrun -t $IMAGE_URL ."
      ]
    }
  }
}
